# Epic 2 Retrospective

## Epic Review

### Scope verified
- Epic: 2
- Review date: 2026-06-27
- Reviewed stories: 2.1, 2.2, 2.3, 2.4
- Completion status: **4/4 stories done**
- Sprint status source: `_bmad-output/implementation-artifacts/sprint-status.yaml`

### Nguồn dữ liệu
- `sprint-status.yaml`
- Story artifacts:
  - `_bmad-output/implementation-artifacts/2-1-micro-sessions-c-s-n-trong-th-vi-n-v-startup-nhanh.md`
  - `_bmad-output/implementation-artifacts/2-2-minimal-timer-v-mindfulness-bell-c-b-n.md`
  - `_bmad-output/implementation-artifacts/2-3-gentle-re-entry-sau-phi-n.md`
  - `_bmad-output/implementation-artifacts/2-4-session-runtime-h-tr-noise-aware-mode-baseline.md`
- Commit lịch sử:
  - `d2c9f52`, `9fb695b`, `4647bafb`, `97e63ba`, `2f3ce89`

### Thành công
- Delivery runtime cho Epic 2 đã hoàn thành đầy đủ 4 story và đạt đồng bộ giữa session UI + timeline theo hướng hệ thống event.
- Micro-startup, timer/bell, re-entry và noise-aware recovery đã đủ coverage trong app code và widget test tương ứng.
- Quy trình dev/testing chủ động (`flutter test` + `flutter analyze`) được dùng thường xuyên ở từng story, giúp phát hiện sớm regression.

### Bài học & điểm lưu ý
- `97e63ba` cho Story 2.4 chỉ sửa BMAD artifacts và `sprint-status`, còn code/test chỉ được bổ sung ở `2f3ce89`.
  - Đây là incident lặp lại từ Story 1.4 và xác nhận lại rủi ro **artifact-only close**.
  - Bài học: close task phải fail nếu không có bằng chứng code + test thay đổi trong cùng closure packet.
- Story 2.4 đã buộc runtime phải chuyển sang **production path** cho quyền micro từ đầu:
  - thêm permission preflight vào luồng session start
  - bổ sung stream/owner để drop enrichment khi quyền bị mất giữa phiên
  - không phụ thuộc toggle dev-only để tiếp tục core flow
- Story 2.4 đã kiểm tra tốt khả năng fallback khi thiếu quyền: manual context từ check-in và low-confidence label.
- Kiểm tra chất lượng vẫn thiên về targeted test; chưa thấy evidence cho full suite chạy trước khi close story/epic.
- Debt kỹ thuật nổi bật chưa được khắc phục: microphone runtime dùng MethodChannel/EventChannel cần follow-up native instrumentation/e2e (đặc biệt khi quyền thay đổi giữa phiên).

### Kiểm lại action từ Epic 1
- Ổn: nhiều lỗi repo-path đã được xử lý, source hiện tại chạy trong `elaro-high`.
- Chưa đóng vòng: các action item Epic 1 vẫn còn mở về review format và anti split packet theo quy ước traceability.

## Next Epic Preparation

### Dự đoán rủi ro cho Epic 3
- Nếu không khóa quy trình đóng story, pattern artifacts-only có thể quay lại ở bất kỳ story nào có thay đổi luồng runtime phức tạp.
- Nếu không chuẩn bị preflight+runtime-path instrumentation rõ ràng cho mic/permission, risk của drift giữa test giả lập và thiết bị thật sẽ tăng.

### Quy trình được khuyến nghị cho Epic 3
- Tách ranh giới đóng story thành một gói xác thực bắt buộc:
  - evidence code/test path
  - status transition đủ bước `ready-for-dev -> in-progress -> review -> done`
  - kiểm tra commit packet đi kèm artifact+code+test trong một context duy nhất.
- Checklist close mới:
  - Ấn định trạng thái `review`/`done` sau khi kiểm tra `apps/mobile` có file code và test thực tế.
  - Build gate mặc định require לפחות một test suite chạy theo pattern team-defined trước close; ít nhất có targeted + smoke for changed flow.
- Đặt `run full suite` là hard requirement trước khi mark story/epic done khi thay đổi runtime/session/timeline hoặc DevGate/permission path.
- Thêm ràng buộc "fail condition" tự động nếu commit đóng story thiếu:
  - file code mới/đã chỉnh sửa
  - file test mới/đã cập nhật cho story
  - thay đổi `sprint-status` hoặc artifact tương ứng

## Commitments
- `2-retrospective`: đánh dấu hoàn thành trong `sprint-status.yaml`.
- Epic 3 action items:
  - `Close packet hard-gate` (status: `open`, owner: `Amelia`)
  - `Add full suite pre-close gate (session/performance/permission critical paths)` (owner: `Murat`)
  - `Complete native microphone permission instrumentation + e2e` (owner: `Winston`)
  - `Formalize Epic 2 close-check failure condition in close template` (owner: `John`)
  - `Track Epic 1 open action items to "done/in progress" with evidence` (owner: `Paige`)
