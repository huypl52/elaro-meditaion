# Epic 5 Retrospective

## Epic Review
- Epic 5 giữ đúng hướng `aggregate/anonymous/community-safe`: trải nghiệm ở `/presence` và các điểm inline trên `Home`/`Growth` không trượt sang social graph, feed, hay social pressure.
- 5.1-5.4 đều khớp với `docs/sprint-ui-map.md`; mapping route/surface rõ ràng, không tạo màn mới ngoài phạm vi đã chốt.
- Pattern tốt nhất là giữ copy tối giản, trung tính, và có đường lui rõ ràng; điều này hợp với mục tiêu quiet presence hơn là tăng tương tác.
- Clear worker sau mỗi story giúp context sạch, giảm drift giữa các story, và làm review/triage dễ hơn.
- Commit sau từng story là bắt buộc; nó chốt trạng thái rõ ràng, giúp trace thay đổi và rollback logic nếu cần.
- Điểm cần cải thiện là tiếp tục giữ cùng một chuẩn trạng thái và handoff ngay từ đầu story, để status file, artifact, và UI map luôn đồng bộ.

## Next Epic Preparation
- Epic 6 phải coi `session timeline immutable` là source of truth cho mọi summary và read model.
- Trước mọi OS prompt cho mic/health, phải có permission sheet giải thích trước.
- `Offline-first` và `sensor fallback` cần là mặc định, không được khóa core flow khi thiếu network hoặc permission.
- Distress flows phải có exit/recovery rõ ràng, tối đa 1-2 thao tác, không làm người dùng mắc kẹt.
- `Error envelope` phải thống nhất (`code`, `message`, `retryable`, `details`) để xử lý lỗi nhất quán và dễ quan sát.
- Accessibility baseline cần được khóa sớm: tap target, VoiceOver, reduce motion, và haptic/text fallback phải có từ đầu.
